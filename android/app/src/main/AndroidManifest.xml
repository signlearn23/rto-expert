<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Internet + network state: required for ads (google_mobile_ads) and
         any future backend sync (moderation queue, question-bank updates). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location: required for "nearby driving schools/RTO offices" and
         google_maps_flutter's current-location button. Fine location is
         requested at runtime by the geolocator plugin; declaring it here
         is still required. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:label="RTO Expert"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- Google Maps API key — replace YOUR_MAPS_API_KEY_HERE before
             release, and restrict this key in Google Cloud Console to this
             app's package name + release SHA-1 fingerprint (see
             android_signing_setup.md). Never leave it unrestricted. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_MAPS_API_KEY_HERE" />

        <!-- AdMob App ID — replace with your real App ID from the AdMob
             console before release. Unit IDs live in
             lib/services/ads_service.dart, not here. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Required by Flutter's embedding v2 tooling (deep links, etc). -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
